# banto-industrial プロジェクト規約（Claude 向け）

産業用データ収集・監視アプリ群（Rust workspace + SvelteKit/Tauri）。
全体計画は [docs/plan.md](docs/plan.md)、タグサーバー banto-hub の設計は
[docs/tag-server-design.md](docs/tag-server-design.md) を参照。

## AI モデルの役割分担（2026-08-05 オーナー指示）

本プロジェクトの AI 作業は、コストと品質の分担として次の2層で行う:

- **実装・ファイル操作 = 下位モデル**: コードの実装、ファイルの作成・編集、
  テスト作成、機械的な一括変更は、Agent ツールで **`model: "sonnet"`** の
  サブエージェントに委譲して実行する（ごく機械的な一括操作は `"haiku"` も
  可）。メインセッションの上位モデルが直接実装編集を行わない。
- **監査・レビュー = 上位モデル**: サブエージェントの成果物のレビュー
  （差分の監査、設計適合性・安全性の確認）、設計判断、オーナーとの議論、
  コミット可否の判断は、メインセッションの上位モデル（Fable / Opus 系）が
  行う。**下位モデルの出力は上位モデルのレビューを通してからコミットする。**

運用の型: 上位モデルがタスクを分解して実装指示を書く → sonnet
サブエージェントが実装 → 上位モデルが差分をレビューし、問題があれば
修正指示を出して再実行 → 合格後にコミット・push。

例外: docs のみの軽微な編集（決定事項の反映など）と git 操作自体は
メインセッションが直接行ってよい。

## 開発規約

- コミットメッセージは日本語・Conventional Commits 風（既存履歴に合わせる）
- 設計判断はオーナー決定として docs に日付付きで記録する
- コードのコメント・ドキュメントの流儀は既存クレートに合わせる
  （クレートの lib.rs 冒頭に設計意図を記述するスタイル）
