# banto-industrial プロジェクト規約（Claude 向け）

産業用データ収集・監視アプリ群（Rust workspace + SvelteKit/Tauri）。
**docs 全体の地図・現状ひとめ・各文書の「正」は [docs/README.md](docs/README.md) を最初に参照。**
全体計画は [docs/plan.md](docs/plan.md)、タグサーバー banto-hub の設計は
[docs/tag-server-design.md](docs/tag-server-design.md)。

## AI モデルの役割分担（2026-08-05 オーナー指示）

本プロジェクトの AI 作業は、コストと品質の分担として次の2層で行う:

- **実装・ファイル操作 = 下位モデル**: コードの実装、ファイルの作成・編集、
  テスト作成、機械的な一括変更は、Agent ツールで **`model: "sonnet"`** の
  サブエージェントに委譲して実行する（ごく機械的な一括操作は `"haiku"` も
  可）。メインセッションの上位モデルが直接実装編集を行わない。
- **監査・レビュー = 上位モデル**: サブエージェントの成果物のレビュー
  （差分の監査、設計適合性・安全性の確認）、設計判断、オーナーとの議論、
  コミット可否の判断は、メインセッションの上位モデル（Fable / Opus 系）が
  行う。**下位モデルの出力は上位モデルのレビューを通してからコミットする。**

- **難易度によるモデル選択（2026-09-06 オーナー追加）**: 上記2層を難易度で
  階調化する。委譲（指示・監査・レビュー・実装のいずれも）は難易度に応じて
  Agent ツールの `model` を選ぶ:
  - **`haiku`**: ごく機械的・低リスク（一括置換、日付/スキーマ/索引の同期、
    docs の軽微編集、grep/集計）。
  - **`sonnet`**: 標準的な実装・テスト作成・中規模リファクタ・パターン展開、
    中程度の監査/レビュー。
  - **上位 `fable` / `opus`**: 難所・高リスク（新規アーキテクチャ、安全/並行/
    原子性に関わる実装、複雑な横断リファクタ、機微な監査・最終レビュー・
    設計判断）。上位は fable（Claude 5.1 系）と opus の2つで、難易度・特性に
    応じて選ぶ。
    機械的作業に上位モデルを使わず、機微な作業に下位モデルを使わない。最終
    レビューとコミット可否は引き続きメインセッション（上位）が担う。

運用の型: 上位モデルがタスクを分解して実装指示を書く → sonnet
サブエージェントが実装 → 上位モデルが差分をレビューし、問題があれば
修正指示を出して再実行 → 合格後にコミット・push。

例外: docs のみの軽微な編集（決定事項の反映など）と git 操作自体は
メインセッションが直接行ってよい。

## 開発規約

- コミットメッセージは日本語・Conventional Commits 風（既存履歴に合わせる）
- **Markdown（docs/ 含む）や TS/Svelte を編集したら、コミット前に必ず
  `pnpm exec prettier --write <対象ファイル>` を実行する**（CI の
  format:check が prettier 準拠を強制している。docs の表編集は特に
  引っかかりやすい）
- 設計判断はオーナー決定として docs に日付付きで記録する
- docs/ に新しい設計・計画ドキュメントを作る、または実装状況が変わる変更を
  入れるときは、対象ドキュメント冒頭の `状態:` 行（`最終検証日:` があれば
  それも）を同じ PR で更新する。ヘッダと本文の実装状況表を矛盾したまま
  放置しない（2026-08-08、docs/improvement-plan.md H8）
- コードのコメント・ドキュメントの流儀は既存クレートに合わせる
  （クレートの lib.rs 冒頭に設計意図を記述するスタイル）
