<script lang="ts">
	/**
	 * 接続カテゴリのルート（#359 chronogazer 分）。markup・state・関数は
	 * ConnectivitySection.svelte（co-located、移動なし）。
	 */
	import ConnectivitySection from '../ConnectivitySection.svelte';
</script>

<div class="sections">
	<ConnectivitySection />
</div>
