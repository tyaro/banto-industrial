<script lang="ts">
	/**
	 * 接続カテゴリのルート（#359 段階2）。markup・state・関数は
	 * ConnectivitySection.svelte（段階1のまま co-located、移動なし）。
	 */
	import ConnectivitySection from '../ConnectivitySection.svelte';
</script>

<div class="sections">
	<ConnectivitySection />
</div>
