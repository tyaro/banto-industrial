<script lang="ts">
	/**
	 * アカウントカテゴリのルート（#359 段階2）。markup・state・関数は
	 * AccountSection.svelte（段階1のまま co-located、移動なし）。
	 */
	import AccountSection from '../AccountSection.svelte';
</script>

<div class="sections">
	<AccountSection />
</div>
