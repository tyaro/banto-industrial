<script lang="ts">
	/**
	 * セキュリティカテゴリのルート（#359 段階2）。markup・state・関数は
	 * SecuritySection.svelte（段階1のまま co-located、移動なし）。
	 */
	import SecuritySection from '../SecuritySection.svelte';
</script>

<div class="sections">
	<SecuritySection />
</div>
