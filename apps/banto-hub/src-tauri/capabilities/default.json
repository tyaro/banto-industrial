{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window - chronogazer と同様 core:default。切替ウィザードの invoke は generate_handler 登録のみ（運転 API の invoke は追加しない）。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
