<script lang="ts">
	/**
	 * データカテゴリのルート（#359 relay-wright 分）。markup・state・関数は
	 * DataSection.svelte（co-located、移動なし）。
	 */
	import DataSection from '../DataSection.svelte';
</script>

<div class="sections">
	<DataSection />
</div>
